import fs from 'fs';
import puppeteer from 'puppeteer';
import { PdfDataExtractor } from 'pdf-data-parser';

(async () => {
    // We already have 422115311 pdf url? Let me just write simple regex to test on string since I don't have the PDF file directly.
    process.exit(0);
})();
